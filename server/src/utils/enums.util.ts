export enum StatusActive {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
}

export enum StatusDelected {
    NOT_DELETED = 'NOT_DELETED',
    DELETED = 'DELETED',
}

export enum UserType {
    ARTIST = 'ARTIST',
    BUSSNESS_PERSON = 'BUSSNESS_PERSON',
}

export enum ErrorCode {
    
}